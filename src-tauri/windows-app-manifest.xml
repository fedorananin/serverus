<!-- Windows application manifest embedded into EVERY link target — the app
     binary and test executables alike (see build.rs). Declares the Common
     Controls v6 side-by-side dependency; without it a binary loads comctl32
     v5 and aborts at startup with STATUS_ENTRYPOINT_NOT_FOUND
     (TaskDialogIndirect exists only in v6). Content mirrors tauri-build's
     windows-app-manifest.xml. -->
<assembly xmlns="urn:schemas-microsoft-com:asm.v1" manifestVersion="1.0">
  <dependency>
    <dependentAssembly>
      <assemblyIdentity
        type="win32"
        name="Microsoft.Windows.Common-Controls"
        version="6.0.0.0"
        processorArchitecture="*"
        publicKeyToken="6595b64144ccf1df"
        language="*"
      />
    </dependentAssembly>
  </dependency>
</assembly>
